{
    "name": "GenderClf",
    "n_gpu": 1,
    "arch": {
        "type": "Model",
        "args": {
            "num_classes": 2,
            "label_name": "gender",
            "pretrained_model": "efficientnet_b1",
            "pretrained_out_feature": 1000
        }
    },
    "data_loader": {
        "type": "MaskImageDataLoader",
        "args": {
            "d_type": "gender",
            "resize": 260,
            "data_dir": "data/input/data/",
            "batch_size": 64,
            "shuffle": true,
            "validation_split": 0.2,
            "num_workers": 3,
            "csv_path": "data/train_csv_with_multi_labels.csv"
        }
    },
    "optimizer": {
        "type": "SGD",
        "args": {
            "lr": 0.01,
            "momentum": 0.9
        }
    },
    "loss": "cross_entropy_loss_gen",
    "metrics": [
        "accuracy",
        "f1"
    ],
    "lr_scheduler": {
        "type": "StepLR",
        "args": {
            "step_size": 50,
            "gamma": 0.1
        }
    },
    "trainer": {
        "epochs": 10,
        "save_dir": "saved/",
        "save_period": 1,
        "verbosity": 2,
        "monitor": "min val_loss",
        "early_stop": 2,
        "tensorboard": true
    }
}